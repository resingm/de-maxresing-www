<header class="header bg-black white">
  <div class="container">
    <div class="row">
      <div class="col c12">
        <h1 class="title">{{ title }}</h1>
        <h3 class="title">{{ subtitle }}</h3>
      </div>
    </div>

    <div class="row">
      <div class="col c12">{{ header.content | safe }}</div>
    </div>
  </div>
</header>


